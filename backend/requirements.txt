Django>=5.1,<6.0
djangorestframework>=3.14,<4.0
django-cors-headers>=4.3,<5.0
django-filter>=24.0
python-decouple>=3.8
psycopg2-binary>=2.9
openpyxl>=3.1
gunicorn>=21.2
dj-database-url>=2.1
requests>=2.31
python-dateutil>=2.9
pytest>=7.4
pytest-django>=4.7
python-amazon-sp-api>=1.8.0
reportlab>=4.0
# django-q2: background worker for the FBA shipment state machine.
# Runs as a sidecar container (`qcluster` service in docker-compose.yml)
# that drives fba_shipments.services.workflow.advance_plan via the Postgres
# ORM broker — no Redis/RabbitMQ dependency.
django-q2>=1.7
# Master Stock sheet integration (push direction). Service-account auth
# lives at /run/secrets/sheets_service_account.json (mounted by compose).
# Used by imports.push_master_stock_sheet to write Manufacture stock
# changes back to the Google Sheet.
gspread>=6.0
google-auth>=2.30
